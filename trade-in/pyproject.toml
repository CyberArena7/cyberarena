[project]
name = "trade-in"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["flask>=3.1.1", "openpyxl>=3.1.5"]

[tool.pyright]
venvPath = "."
venv = ".venv"
